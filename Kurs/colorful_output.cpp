#include "common.h"
// Файл: colorful_output.cpp
// Содержит функции для цветного вывода в консоль
// Поддерживает форматированный вывод с переменным числом аргументов
//---------------------------------------------------------------------------------------------------------------------------------------------------------
// Функция изменения цвета текста в консоли
// Аргументы: int color - код цвета
void set_console_color(int color) {
    SetConsoleTextAttribute(GetStdHandle(STD_OUTPUT_HANDLE), color); // Получаем дескриптор стандартного вывода и устанавливаем атрибуты текста
}
//---------------------------------------------------------------------------------------------------------------------------------------------------------
// Функция сброса цвета текста в консоли 
void reset_console_color() {
    SetConsoleTextAttribute(GetStdHandle(STD_OUTPUT_HANDLE), WHITE_CODE); // Делаем цвет текста в консоли белым
}
//---------------------------------------------------------------------------------------------------------------------------------------------------------
// Функция для печати сообщений об ошибке (красный цвет)
// Аргументы: const char* format - строка формата, ... - переменное количество аргументов для подстановки в формат 
void print_error(const char* format, ...) {
    set_console_color(RED_CODE); 
    va_list args; // Указатель на список аргументов
    va_start(args, format); // Инициализируем args после format
    vprintf(format, args); // Форматированный вывод в консоль
    va_end(args); // Завершаем работу со списком аргументов
    reset_console_color();
}
//---------------------------------------------------------------------------------------------------------------------------------------------------------
// Функция для печати сообщений о предупреждении (желтый цвет)
// Аргументы: const char* format - строка формата, ... - переменное количество аргументов для подстановки в формат 
void print_warning(const char* format, ...) {
    set_console_color(YELLOW_CODE); 
    va_list args;
    va_start(args, format);
    vprintf(format, args);
    va_end(args);
    reset_console_color();
}
//---------------------------------------------------------------------------------------------------------------------------------------------------------
// Функция для печати сообщений об успехе (зеленый цвет)
// Аргументы: const char* format - строка формата, ... - переменное количество аргументов для подстановки в формат 
void print_success(const char* format, ...) {
    set_console_color(GREEN_CODE); 
    va_list args;
    va_start(args, format);
    vprintf(format, args);
    va_end(args);
    reset_console_color();
}
//---------------------------------------------------------------------------------------------------------------------------------------------------------
// Функция для печати информационных сообщений (голубой цвет)
// Аргументы: const char* format - строка формата, ... - переменное количество аргументов для подстановки в формат 
void print_info(const char* format, ...) {
    set_console_color(CYAN_CODE); 
    va_list args;
    va_start(args, format);
    vprintf(format, args);
    va_end(args);
    reset_console_color();
}
//---------------------------------------------------------------------------------------------------------------------------------------------------------
